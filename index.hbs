{{!< default}}

<div class="small">
	<nav>
		<div>latest posts...</div>
		        {{#foreach posts}}
			    <a href={{url}}>{{title}}</a>
			{{/foreach}}
	</nav>
</div>

<div class="large">
	{{#if pages}}
		Pages are available
	{{/if}}
</div>